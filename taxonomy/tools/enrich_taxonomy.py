#!/usr/bin/env python3
"""enrich_taxonomy.py — обогащение taxonomy через Claude API.

Placeholder. Оригинальный скрипт пользователя ждёт доставки в репо
(см. CLAUDE.md, раздел 'Что СЕЙЧАС сломано / в работе'). После доставки —
заменить содержимое этого файла оригиналом и зафиксировать ADR со
ссылкой на используемую модель Claude и шаблон промпта.
"""

from __future__ import annotations

import sys


def main() -> int:
    raise NotImplementedError(
        "enrich_taxonomy.py — оригинал пользователя пока не доставлен в репо. "
        "См. CLAUDE.md (раздел 'Что СЕЙЧАС сломано / в работе')."
    )


if __name__ == "__main__":
    sys.exit(main())
