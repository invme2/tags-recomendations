#!/usr/bin/env python3
"""build_taxonomy.py — пересборка taxonomy.json из черновых источников.

Stub. Будет реализован когда у нас появятся черновики в `taxonomy/data/`
(`v3.1-source.md`, `v3.2-brainstorm.py`).
"""

from __future__ import annotations

import sys


def main() -> int:
    raise NotImplementedError(
        "build_taxonomy.py пока не реализован. См. CLAUDE.md (раздел 'Что СЕЙЧАС сломано / в работе')."
    )


if __name__ == "__main__":
    sys.exit(main())
